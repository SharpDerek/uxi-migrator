jQuery(document).ready(function($) {
  
  /*
  // for text replacement on form inputs
  function textReplacement(input) {
    var originalvalue = input.val();
    input.focus(function() {
      if($.trim(input.val()) == originalvalue){ input.val(""); }
    });
    input.blur(function() {
      if($.trim(input.val()) == ""){ input.val(originalvalue); }
    });
  }
  textReplacement($("#textinput1"));
  */
  /*
  // loads only if the cycle selector exists
  if ($(".flexslider").length > 0){ 
    $(window).load(function() {
      $('.flexslider').flexslider({
        animation         : 'fade',
        prevText          : '<i class="icon-mw-arrow-left-3"></i><span class="visuallyhidden">previous</span>',
        nextText          : '<i class="icon-mw-arrow-right-3"></i><span class="visuallyhidden">next</span>',
        directionNav: true,
        controlNav: true
      });
    });
  };
  */
  /*
  $('.gallery-item a').attr('rel','gallery');
  $('.gallery-item a[rel="gallery"]').fancybox({
    padding : 0,
    margin : 20,
    prevEffect : 'fade',
    nextEffect : 'fade',
    closeBtn : false,
    arrows : false,
    helpers   : { 
      title : null,
      buttons : {}
    }
  });
  */  

});/* end document reday function */